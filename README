see hours_per_project.py -h for usage

see *.example for example config

per project recipients only working if -p option used
I will fix this

pygal is needed for the chart option
Make sure you have texlive and pdflatex installed if you want to use the invoice generator
